import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cml.versionChecker',
  appName: 'android',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
